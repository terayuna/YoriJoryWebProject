<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.yorijory.webprj.dao.NoticeDao">
	<select id="getNotices" resultType="com.yorijory.webprj.vo.Notice">
	
		SELECT * FROM(
         SELECT 
            ROW_NUMBER() OVER (ORDER BY REGDATE DESC) NUM, 
            NOTICES. * FROM NOTICES 
            WHERE ${param2} LIKE '%${param3}%'
            ) A 
            WHERE NUM BETWEEN 1+(#{param1}-1)*10 AND #{param1}*10
		 
	 </select>
	 
	  <update id="update">
		UPDATE NOTICES SET TITLE=#{title}, CONTENT=#{content} WHERE MID=#{mid}
	 </update> 
	 
	 <insert id="insert">
	 	INSERT INTO MESSAGES(MID, TITLE, CONTENT) VALUES(#{mid},#{title},#{content})
	 </insert>
	 	
	 <delete id="delete">
	 	DELETE NOTICES WHERE MID=#{mid}
	 </delete>
	 
	
</mapper>